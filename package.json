{
  "name": "playniti-monorepo",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "start": "turbo run start",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.12",
    "typescript": "^5.4.0",
    "eslint": "^9.0.0",
    "@types/node": "^20.11.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "infra/*"
  ]
}